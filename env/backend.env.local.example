## MV-OS Backend — Local (SAFE)
## Rules:
## - Never use production credentials locally
## - Local must use mock providers (NODE_ENV=local)

NODE_ENV=local
PORT=3000
FRONTEND_URL=http://localhost:3001

# Local Postgres (Docker Compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mv_os?schema=public

# Local-only JWT secret (do not reuse)
JWT_SECRET=local-dev-change-me-min-32-chars
JWT_EXPIRES_IN=7d


