## MV-OS Backend — Staging
## Rules:
## - Use test credentials only (never production)
## - Separate database from production
## - NODE_ENV must be staging

NODE_ENV=staging
PORT=3000
FRONTEND_URL=https://staging.mv-os.example.com

# Staging DB (RDS / separate instance)
DATABASE_URL=postgresql://postgres:REDACTED@staging-db.example.us-east-1.rds.amazonaws.com:5432/mv_os_staging?schema=public

# Staging-only JWT secret (do not reuse)
JWT_SECRET=staging-change-me-min-32-chars
JWT_EXPIRES_IN=7d

# Integrations (test accounts)
# Zoho Email credentials are stored in DB via Settings -> Integrations
# SMSMisr credentials are stored in DB via Settings -> Integrations
# WhatsApp/Meta credentials are stored in Marketing Channel Accounts (test accounts)


