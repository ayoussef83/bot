## MV-OS Backend — Production
## Rules:
## - Live credentials only
## - Separate DB from staging
## - NODE_ENV must be production

NODE_ENV=production
PORT=3000
FRONTEND_URL=https://mv-os.mvalley-eg.com

# Production DB (RDS)
DATABASE_URL=postgresql://postgres:REDACTED@prod-db.example.us-east-1.rds.amazonaws.com:5432/mv_os?schema=public

# Production-only JWT secret (do not reuse)
JWT_SECRET=prod-change-me-min-32-chars
JWT_EXPIRES_IN=7d


